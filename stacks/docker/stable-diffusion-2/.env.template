# Minimal env for stable-diffusion-2
# Copy to .env and adjust values as needed.

# Host port to expose the WebUI
SD_WEBUI_PORT=8080

# Base data directory on the host. Subfolders are created/mounted for:
# inputs, textual_inversion_templates, embeddings, extensions, models,
# localizations, outputs
SD_DATA_DIR=/srv/stable-diffusion

